# 手动时间选择功能使用说明

## 功能概述

新增手动时间选择功能，允许用户手动设置照片拍摄时间，方便测试和验证 overlay 效果，不受 EXIF 时间准确性限制。

## 使用方法

### 步骤 1: 选择 FIT 文件
首先选择 FIT 文件，应用会解析并显示：
- FIT 文件开始时间
- FIT 文件结束时间
- GPS 点数量等信息

### 步骤 2: 选择照片
选择照片后，应用会：
- 尝试读取 EXIF 时间（自动）
- 将 EXIF 时间设置为默认手动时间

### 步骤 3: 使用时间选择控件

在 "Time Selection" 区域：

**显示内容：**
- **EXIF Time**: 从照片读取的时间
- **Use Manual Time**: 复选框，控制使用哪个时间
- **当前时间显示**: 
  - 启用手动时间：显示当前手动选择的时间
  - 未启用手动时间：显示可用的手动时间

**切换时间来源：**
1. **使用 EXIF 时间（默认）**：
   - 保持复选框 "Use Manual Time" 未勾选
   - 应用自动使用照片 EXIF 时间匹配 GPS

2. **使用手动时间**：
   - 勾选复选框 "Use Manual Time"
   - 点击 "Change" 按钮
   - 在日期选择器中选择日期（限制在 FIT 时间范围内）
   - 在时间选择器中选择时分
   - 应用会立即使用新时间匹配 GPS

### 步骤 4: 查看匹配结果

匹配成功后：
- 查看 GPS 匹配日志（控制台）
- 查看匹配点详情（UI）
- 查看图片上的 overlay

## UI 显示说明

**时间选择区域背景色：**
- 蓝色背景：时间选择控件区域

**当前使用时间指示：**
- 🟢 绿色背景：使用 EXIF 时间
- 🟠 橙色背景：使用手动时间

## Debug 日志输出

### 时间设置日志
```
=== Manual Time Set ===
Selected time: 2016-04-19 09:30:00.000
Using manual time: true
=== Manual Time End ===
```

### 时间源日志
```
=== Time Source ===
Using: Manual Time (或 EXIF Time)
Time value: 2016-04-19 09:30:00.000
=== Time Source End ===
```

### GPS 匹配日志
见之前说明的 `GPS Matching Debug` 部分

## 快速测试 Overlay 的步骤

1. **选择 FIT 文件**（如 `22576860731_ACTIVITY.fit`）
2. **选择任意照片**（即使 EXIF 时间不对）
3. **勾选 "Use Manual Time"**
4. **点击 "Change" 按钮**
5. **选择一个在 FIT 时间范围内的时间**
6. **立即查看图片 overlay 效果**

## 常见测试场景

### 场景 1: 验证 overlay 是否显示
- 选择 FIT 文件和照片
- 手动选择 FIT 文件中间的时间点
- 查看图片上是否显示轨迹线和位置点

### 场景 2: 测试不同时间点的 overlay
- 在 FIT 时间范围内多次切换时间
- 观察位置点在不同轨迹位置的变化
- 查看配速信息的变化

### 场景 3: 验证时间匹配逻辑
- 手动选择精确时间
- 查看匹配日志中的时间差
- 验证是否在 5 秒容忍度内精确匹配

### 场景 4: 测试时区问题
- 对比 EXIF 时间和手动设置相同时间
- 如果结果不同，说明时区转换有问题

## 限制和注意事项

1. **时间范围限制**：
   - 手动时间只能在 FIT 文件的开始和结束时间范围内选择
   - 日期选择器会限制可选日期
   - 时间选择器可选择任意时分

2. **秒数设置**：
   - 手动时间只能选择日期和时分
   - 秒数默认为 0 或保留上次设置的秒数
   - GPS 匹配容忍度为 5 秒，秒数差异不影响匹配

3. **需要先选择 FIT 文件**：
   - 如果没有 FIT 文件，点击 "Change" 会提示错误
   - FIT 文件提供时间范围限制

4. **切换时间源**：
   - 切换复选框会立即触发重新匹配
   - 不需要重新选择照片或 FIT 文件

## 优势

✅ 不依赖 EXIF 时间准确性  
✅ 快速验证 overlay 功能  
✅ 方便调试时间匹配逻辑  
✅ 可测试任意时间点的效果  
✅ 实时切换时间源  
✅ 详细日志辅助诊断  

## 与 EXIF 时间的关系

- 两种时间源可以并存
- 默认使用 EXIF 时间（自动）
- 可以随时切换到手动时间
- 手动时间初始值来自 EXIF（如果存在）
- 可以禁用手动时间，恢复使用 EXIF

## 下一步建议

测试成功后，可以：
1. 确认 overlay 显示正常
2. 检查时间匹配准确
3. 调整 overlay 样式（颜色、宽度等）
4. 保存当前配置
5. 导出带 overlay 的图片（待开发功能）